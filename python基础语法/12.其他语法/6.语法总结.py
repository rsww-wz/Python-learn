"""
python关键字
    False
    None
    True
    and
    as
    assert：断言
    break
    class
    continue
    def
    del
    elif
    else
    except：异常处理
    finally：异常处理
    for
    from 
    global ：声明全局变量
    if 
    import 
    in 
    is 
    lambda ：特殊的定义函数的形式，匿名函数
    nonlocal ：声明外部嵌套函数内的变量，只在闭包里面生效，作用域就是闭包里面的
    not 
    or 
    pass 
    raise ：异常处理，可以通过raise显示地引发异常。
            一旦执行了raise语句，raise后面的语句将不能执行
    return 
    try：异常处理
    while 
    with ：文件处理
    yield：生成器在遍历时候逐个返回，遍历的过程就是调用next()的过程
            好处就在于使用时候才产生数据，消耗内存
"""

